# Sample data for ai-103-agent-knowledge

Drop these files here to exercise the file search + CU tools:

- `product-catalog.pdf` — any product catalog PDF (uploaded into the vector store)
- `invoice.pdf` — a sample invoice (passed to the Content Understanding tool)

The agent code skips the upload steps gracefully when files are missing.
