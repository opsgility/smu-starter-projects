# sample_data/

Contents used across the three Lesson 9 exercises:

| File                         | Used by                       | Notes                                                     |
| ---------------------------- | ----------------------------- | --------------------------------------------------------- |
| `product-catalog.pdf`        | Exercise 1 — FileSearchTool   | Real PDF expected. See `product-catalog.pdf.md`.          |
| `invoice.pdf`                | Exercise 3 — extract_invoice  | Real PDF expected. See `invoice.pdf.md`.                  |
| `summitline-returns.md`      | Exercise 2 — summitline-kb    | Seed document for the `summitline-kb` Search index.       |
| `summitline-warranty.md`     | Exercise 2 — summitline-kb    | Seed document for the `summitline-kb` Search index.       |
| `summitline-pickup.md`       | Exercise 2 — summitline-kb    | Seed document for the `summitline-kb` Search index.       |

The two `*.pdf.md` files document what each real PDF contains. They sit
next to the real PDFs as a regeneration spec — if you ever need to rebuild
the PDFs, the markdown tells you exactly what content to include so the
exercises still ground correctly.
