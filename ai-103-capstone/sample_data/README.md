# Capstone sample data

Drop these here to fully exercise `test_client.py`:

- `storefront.jpg` — any storefront image (used by `/vision-ask`)
- `invoice.pdf` — a sample invoice (used by `/extract-doc`)

Missing assets are skipped by the test client.
