name: Deploy Foundry infra
on:
  push:
    branches: [main]
    paths:
      - 'infra/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO 1: Sign in with Azure using OIDC.
      #         Use azure/login@v2 with client-id, tenant-id, subscription-id from repo secrets.

      # TODO 2: Validate the template first.
      #         az deployment group validate --resource-group ... --template-file infra/main.bicep --parameters infra/main.parameters.json

      # TODO 3: Deploy the template.
      #         az deployment group create --resource-group ... --template-file infra/main.bicep --parameters infra/main.parameters.json
