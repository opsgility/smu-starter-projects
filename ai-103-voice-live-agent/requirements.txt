# All of these are ALREADY INSTALLED in the python-ai container.
# This file is documentation of the packages this lab uses — it is NOT
# something you run `pip install` on. `pip install` in the container is
# forbidden by lab policy (hard constraint 9 of the author skill).
# If a package listed here is missing at runtime, the container image is
# behind — flag it in the assistant panel; do not work around it.

azure-ai-voicelive[aiohttp]==1.3.0b1
azure-ai-projects
azure-ai-agents
azure-search-documents
azure-identity
python-dotenv
