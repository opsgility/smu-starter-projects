# Reference manifest — every package here is already installed in the python-ai container variant.
# Students do NOT run pip install. The Dockerfile installs from this file when building the container
# image that ships to Azure Container Apps (which is a slim base without the lab preinstalls).
azure-identity>=1.19.0
azure-ai-projects>=1.0.0
azure-monitor-opentelemetry>=1.6.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0
