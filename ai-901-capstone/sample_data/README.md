# Capstone sample data

Drop these assets here to run `test_client.py` end-to-end:

- `storefront.jpg` — any storefront or retail image
- `invoice.pdf` — a simple supplier invoice

Missing assets are skipped by the test client so partial runs still work.
