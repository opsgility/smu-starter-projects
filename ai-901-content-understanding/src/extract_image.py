"""Image extraction stub — exercise has you wire the same pattern as extract_document.py."""
from __future__ import annotations


def extract_from_image(path: str) -> dict:
    """TODO (exercise): POST image bytes to kind=images, poll, return result."""
    raise NotImplementedError("Exercise step: mirror extract_document.py for images.")


if __name__ == "__main__":
    print("Run the document extractor first, then come back to implement this stub.")
