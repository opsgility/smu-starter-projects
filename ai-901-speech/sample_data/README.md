# Speech sample data

During the lab you'll record your own `question.wav` using the Speech SDK, or download a sample from Microsoft Learn. The exercise instructions walk you through both options.

Place audio here as `question.wav` before running `voice_loop.py`.
