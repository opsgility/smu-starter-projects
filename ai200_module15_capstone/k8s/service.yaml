apiVersion: v1
kind: Service
metadata:
  name: northwind-api
  labels:
    app: northwind-api
spec:
  type: LoadBalancer
  selector:
    app: northwind-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
