# lab02-defaults-query — CLI defaults, query, and ID extraction

**AZ-ADMIN-100 lab 2 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-100 lab-testing pass. When authored, this folder should include:

- `helpers.sh` — sourceable functions for the `az config set` defaults, `--query` patterns, and pipeline-friendly ID extraction taught in the exercise.
- Optional `.gitignore` and any per-lab config files.
