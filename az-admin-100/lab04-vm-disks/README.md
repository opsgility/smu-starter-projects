# lab04-vm-disks — VM + disk lifecycle from the CLI

**AZ-ADMIN-100 lab 4 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-100 lab-testing pass. When authored, this folder should include:

- `helpers.sh` — sourceable functions for `az vm create`, disk snapshot, and clone-from-snapshot flows taught in the exercise.
- Optional `.gitignore`.
