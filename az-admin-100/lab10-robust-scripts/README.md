# lab10-robust-scripts — Robust admin scripts, GitHub Actions, OIDC cross-sub

**AZ-ADMIN-100 lab 10 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-100 lab-testing pass. When authored, this folder should include:

- `helpers.sh` — sourceable bash toolkit taught in the exercise (error handling wrappers, idempotence guards, cross-subscription helpers).
- `.github/workflows/` — GitHub Actions workflow using OIDC federation for cross-subscription runs.
- `.gitignore`.
