# lab-02-azd-bootstrap — Bootstrap an azd project with hub VNet + Log Analytics baseline

**AZ-ADMIN-200 lab 2 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-200 lab-testing pass. When authored, this folder should include:

- `azd-lz/azure.yaml` — azd project manifest (skeleton committed as the starting point).
- `azd-lz/infra/main.bicep` — hub VNet + Log Analytics workspace baseline.
- `azd-lz/infra/main.bicepparam` — parameter set the student edits during the lab.
- `.gitignore`.
