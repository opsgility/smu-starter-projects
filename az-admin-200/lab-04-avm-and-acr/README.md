# lab-04-avm-and-acr — Compose the LZ with AVM modules + private ACR registry

**AZ-ADMIN-200 lab 4 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-200 lab-testing pass. When authored, this folder should include:

- `infra/main.bicep` — top-level template composing Azure Verified Modules (AVM) for the LZ.
- `infra/modules/` — thin wrapper modules over AVM `br/public:` references.
- `bicepconfig.json` — module aliases including the private ACR (`br:<acr>.azurecr.io/bicep/modules/*`).
- `.gitignore`.
