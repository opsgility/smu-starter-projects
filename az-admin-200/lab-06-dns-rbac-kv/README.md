# lab-06-dns-rbac-kv — Private DNS + RBAC baseline + shared Key Vault

**AZ-ADMIN-200 lab 6 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-200 lab-testing pass. When authored, this folder should include:

- `infra/main.bicep` — private DNS zones, VNet links, and Key Vault with private endpoint.
- `infra/rbac.bicep` — CAF-aligned RBAC baseline (Reader/Contributor/PrivEndpointConnectionApprover at the appropriate scopes).
- `.gitignore`.
