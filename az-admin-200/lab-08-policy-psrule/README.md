# lab-08-policy-psrule — CAF Azure Policy assignments + PSRule gating

**AZ-ADMIN-200 lab 8 starter — authoring pending.**

Placeholder scaffold. Content will land in a follow-up PR authored per the AZ-ADMIN-200 lab-testing pass. When authored, this folder should include:

- `infra/policies/main.bicep` — CAF-aligned initiative assignments at subscription scope.
- `ps-rule.yaml` — PSRule configuration wired to the Azure.Default baseline.
- `.github/workflows/psrule.yml` — CI job that fails the LZ deploy on PSRule violations.
- `.gitignore`.
