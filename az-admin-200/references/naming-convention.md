# AZ-ADMIN-200 — Resource naming convention

**Reference doc for course 600 (AZ-ADMIN-200: Enterprise Landing Zone with Bicep and azd) — authoring pending.**

Placeholder. This file will document the CAF-aligned resource naming scheme the labs use so every lab-*/ starter references the same conventions:

- Resource-type abbreviations (`rg`, `vnet`, `snet`, `nsg`, `kv`, `pe`, `pdz`, `agw`, etc.).
- Environment tokens (`hub`, `spoke`, `mgmt`, `identity`, `connectivity`, `landing`).
- Region tokens and length limits per resource type.
- Worked examples for the hub VNet, shared Key Vault, and private-endpoint records introduced in lab-02.
