# az-dev-160-topic-filters

Multi-tenant SB topic with three SQL filter subscriptions.

## Modes

```
dotnet run -- setup-rules
dotnet run -- publish
dotnet run -- read acme-sub
dotnet run -- read contoso-sub
dotnet run -- read priority-sub
```
