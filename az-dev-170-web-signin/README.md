# az-dev-170-web-signin

ASP.NET Core 10 Razor Pages app with Entra ID sign-in via Microsoft.Identity.Web.

Set `AzureAd:*` in appsettings.json and optionally `Redis:Connection` for distributed token cache.
