# az-dev-170-wif

GitHub Actions → Azure via workload identity federation. Deploys `infra/main.bicep` with no client secret in secrets, only `AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`.
