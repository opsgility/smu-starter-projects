# az-dev-180-multi-import

Three sample backends: minimal-API Customers (.NET), a Function App (created via `az functionapp` during the lab), and a Node GraphQL server.

Run:
```
dotnet run          # ASP.NET Core Customers
node graphql-server.js
```
