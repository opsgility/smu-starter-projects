<policies>
  <inbound><base /></inbound>
  <backend><base /></backend>
  <outbound>
    <base />
    <log-to-eventhub logger-id="orders-logger">
      @{
        return new JObject(
          new JProperty("path",    context.Request.Url.Path),
          new JProperty("status",  context.Response.StatusCode),
          new JProperty("elapsed", context.Elapsed.TotalMilliseconds)
        ).ToString();
      }
    </log-to-eventhub>
  </outbound>
  <on-error><base /></on-error>
</policies>
