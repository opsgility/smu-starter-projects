# az-dev-190-service-health-workbook

Anchorline Orders service-health workbook template (`workbook.json`). Import via App Insights → Workbooks → New → Advanced Editor and paste. Parameters: `TimeRange` (default 1h) and `ApiName` (default anchorline-orders-slo).
