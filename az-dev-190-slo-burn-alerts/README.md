# az-dev-190-slo-burn-alerts

Anchorline Orders SLO demo with a toggleable latency injector so you can drive the burn-rate alerts. The `burn-alerts.kql` file contains the fast (2%/1h) and slow (5%/6h) burn-rate queries to attach as log alerts against the workspace.
