namespace TeamTrackr.Models;

public enum UserRole { Owner, Admin, Member, Viewer }

public enum TaskItemStatus { Todo, InProgress, InReview, Done }

public enum TaskPriority { None, Low, Medium, High, Critical }
