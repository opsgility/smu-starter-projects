# Lab container default — code-server proxies port 3000 at /proxy/3000.
# Set both to "" if running through ngrok / web-tunnel which serves at root.
BASE_PATH=/proxy/3000
NEXT_PUBLIC_BASE_PATH=/proxy/3000
