import { test } from "vitest";

// Fill these in once you've built your tool, resource, and prompt for your chosen track.
// At least 3 passing tests are required for capstone validation.

test.todo("tool handler returns structuredContent matching its outputSchema");

test.todo("resource read returns the expected mimeType and content");

test.todo("prompt template renders messages with the supplied args");
