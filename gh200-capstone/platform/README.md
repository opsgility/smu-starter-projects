# apex-platform

Platform team's shared CI/CD assets for Apex Drone Delivery. Service teams call the reusable workflows in `.github/workflows/` and the composite actions in `actions/` by referencing this repo's `@v1` tag.
