# Git Core Concepts

Practice working with repositories, commits, and branches.

## Exercises

- Create and switch between branches
- Make commits on different branches
- Merge branches and resolve conflicts
- Use `git log`, `git diff`, and `git stash`
