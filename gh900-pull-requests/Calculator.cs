namespace CloudNest;

/// <summary>
/// Basic calculator. Students will create pull requests to add new operations.
/// </summary>
public class Calculator
{
    public double Add(double a, double b) => a + b;

    public double Subtract(double a, double b) => a - b;

    // Students: Create a PR to add Multiply
    // Students: Create a PR to add Divide (with zero-check)
    // Students: Create a PR to add Power/Exponent
}
