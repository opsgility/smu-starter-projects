package com.skillmeup.inventory;

public class StockAlert {
    // TODO Capstone Exercise 1: Add fields:
    //   private Product product
    //   private int threshold
    //   private int currentQuantity
    // Add constructor StockAlert(Product product, int threshold)
    //   that sets currentQuantity = product.getQuantity()
    // Add getters and a isTriggered() method returning currentQuantity < threshold
    // Add toString() returning: "ALERT: " + product.getName() + " qty=" + currentQuantity + " (threshold " + threshold + ")"
}
