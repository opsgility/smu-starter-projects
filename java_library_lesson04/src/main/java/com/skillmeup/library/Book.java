package com.skillmeup.library;

public class Book {
    // TODO Exercise 1: Declare five private fields:
    //   String title, String author, String isbn, int year, boolean available

    // TODO Exercise 2: Add a constructor that accepts all five values and assigns them.

    // TODO Exercise 3: Add getters: getTitle(), getAuthor(), getIsbn(), getYear(), isAvailable()
    // and a toString() returning: title + " by " + author + " (" + year + ") [ISBN: " + isbn + "] - Available: " + available
}
