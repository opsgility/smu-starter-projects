package com.skillmeup.library;

/**
 * TODO Exercise 1: Define the Loanable interface with two method signatures:
 *   void checkOut(Patron patron);
 *   void returnItem();
 * This is an interface — no method bodies, just signatures.
 */
public interface Loanable {
    // TODO: add the two method signatures here
}
