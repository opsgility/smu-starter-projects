package com.skillmeup.library;

public class Patron {
    // TODO Exercise 2: Add three private fields: String name, String patronId, String email.

    // TODO Exercise 3: Add constructor Patron(String name, String patronId, String email)
    // and getters getName(), getPatronId(), getEmail().
    // Add toString() returning: "Patron[" + name + ", " + patronId + ", " + email + "]"
}
