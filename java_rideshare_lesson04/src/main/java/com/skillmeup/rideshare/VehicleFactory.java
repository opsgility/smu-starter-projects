package com.skillmeup.rideshare;

public class VehicleFactory {

    // TODO Exercise 1: Implement createVehicle(String type, String id, String plate)
    // - if type.equalsIgnoreCase("car")        return new Car(id, plate)
    // - if type.equalsIgnoreCase("van")        return new Van(id, plate)
    // - if type.equalsIgnoreCase("motorcycle") return new Motorcycle(id, plate)
    // - otherwise throw IllegalArgumentException("Unknown vehicle type: " + type)
    public static Vehicle createVehicle(String type, String id, String plate) {
        return null; // TODO: implement
    }
}
