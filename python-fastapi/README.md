# Python FastAPI Application

A FastAPI web application starter project.

## Getting Started

Run the application:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

The app will be available at http://localhost:8000

API docs at http://localhost:8000/docs
